import {
	INode,
	ENodeOperationStatus,
	EError,
	ErrnoException,
	IConflictSettings,
	EResolve,
	INodeReport,
	IStatOptions
} from './interfaces.js'

import {  } from  './utils/stats.js'

export const sync = (path: string, options: IStatOptions ) => {
}

export const async = async (path: string, options: IStatOptions ) => {
}
